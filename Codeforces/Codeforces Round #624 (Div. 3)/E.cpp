#include <bits/stdc++.h>

#define R register
#define ll long long
#define sum(a, b, mod) (((a) + (b)) % mod)

const int MaxN = 5e3 + 10;

int n, d;
int f[MaxN], dep[MaxN];

int main()
{   
    int T;
    scanf("%d", &T);
    while(T--)
    {
        scanf("%d%d", &n, &d);
        
    }
    return 0;
}
