#include <bits/stdc++.h>

#define R register
#define ll long long
#define sum(a, b, mod) (((a) + (b)) % mod)

int main()
{
    unsigned long long csp_s_2019_rp = -1;
    printf("%llu", csp_s_2019_rp);
    return 0;
}
