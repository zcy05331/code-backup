#include <bits/stdc++.h>

#define ll long long
#define sum(a, b, mod) (((a) + (b)) % mod)

const int MaxN = 1e5 + 10;
const double delta = 0.725;

struct node
{
    int x, y, val;
};

int n, xl, xr, yl, yr, cur, root;

struct kdtree
{
}

int main()
{

    return 0;
}
