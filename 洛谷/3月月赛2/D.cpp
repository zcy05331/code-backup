#include <bits/stdc++.h>
#define R register
#define ll long long
#define cmin(a, b) ((a < b) ? a : b)
#define cmax(a, b) ((a < b) ? b : a)

int main()
{   
    int T;
    scanf("%d", &T);
    for(int i = 1; i <= T; i++)
        printf("1\n");
    return 0;
}
